import { AdminSectionStub } from './AdminSectionStub';

export function AdminSyncStatusPage() {
  return (
    <AdminSectionStub
      title="Sync Receipt Status"
      description="Server-side submission receipt counts and stale collector alerts."
      issueRef="frontend issue 009"
    />
  );
}
